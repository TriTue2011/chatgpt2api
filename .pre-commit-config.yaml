# Pre-commit hooks — chặn secret NGAY tại máy trước khi commit (offline, tức thì).
# Cài: pipx install pre-commit && pre-commit install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        args: ["--config", ".gitleaks.toml"]
